
import React from 'react';
import PlaceholderPage from './PlaceholderPage';
import { Tag } from 'lucide-react';

const DealsPage: React.FC = () => {
  return <PlaceholderPage title="Top Deals" icon={<Tag size={24} />} />;
};

export default DealsPage;
