
import React from 'react';
import PlaceholderPage from './PlaceholderPage';
import { Mail } from 'lucide-react';

const MailPage: React.FC = () => {
  return <PlaceholderPage title="Mail" icon={<Mail size={24} />} />;
};

export default MailPage;
